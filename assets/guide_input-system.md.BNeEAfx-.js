import{_ as t,o as a,c as s,a2 as o}from"./chunks/framework.BYlK8ftn.js";const k=JSON.parse('{"title":"Input system","description":"","frontmatter":{},"headers":[],"relativePath":"guide/input-system.md","filePath":"guide/input-system.md","lastUpdated":1782027789000}'),n={name:"guide/input-system.md"};function d(i,e,r,c,l,h){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="input-system" tabindex="-1">Input system <a class="header-anchor" href="#input-system" aria-label="Permalink to &quot;Input system&quot;">​</a></h1><p>The input layer lives in <a href="https://github.com/Flopsstuff/korovany/tree/main/src/game/input" target="_blank" rel="noreferrer"><code>src/game/input/</code></a> and turns raw keyboard/mouse events into a <strong>data-driven, rebindable intent</strong> — a small snapshot of what the player wants to do each frame. It is split so the decision logic is pure and unit-testable, with a thin DOM adapter on top.</p><h2 id="layers" tabindex="-1">Layers <a class="header-anchor" href="#layers" aria-label="Permalink to &quot;Layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>intent.ts</code></td><td>Pure model: <code>inputReducer</code> + <code>toIntent</code>. No DOM, no Babylon.</td></tr><tr><td><code>bindings.ts</code></td><td>Data-driven key map (<code>KeyBindings</code>), reverse lookup, and <code>rebind</code>.</td></tr><tr><td><code>domAdapter.ts</code></td><td>Thin wrapper: wires <code>window</code>/<code>document</code> events and Pointer Lock.</td></tr><tr><td><code>index.ts</code></td><td>Public barrel.</td></tr></tbody></table><p>The pure model never imports the DOM or Babylon, so it runs in Vitest without a real browser or GPU. The adapter is the only place that touches <code>window</code>, <code>document</code>, or the canvas.</p><h2 id="intent-shape" tabindex="-1">Intent shape <a class="header-anchor" href="#intent-shape" aria-label="Permalink to &quot;Intent shape&quot;">​</a></h2><p><code>toIntent(state)</code> returns:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>moveX</code></td><td>Strafe axis, normalised: <code>+1</code> right, <code>-1</code> left, <code>0</code>.</td></tr><tr><td><code>moveY</code></td><td>Forward axis: <code>+1</code> forward, <code>-1</code> back, <code>0</code>.</td></tr><tr><td><code>jump</code> / <code>sprint</code></td><td>Booleans, true while held.</td></tr><tr><td><code>lookDX</code> / <code>lookDY</code></td><td>Accumulated mouse-look delta (raw px) since last sample.</td></tr></tbody></table><p>Opposing keys (e.g. forward + back) cancel to <code>0</code>. The game loop calls <code>controller.sample()</code> once per frame, which returns the intent and <strong>clears the accumulated look delta</strong> so deltas never double-count.</p><h2 id="default-bindings" tabindex="-1">Default bindings <a class="header-anchor" href="#default-bindings" aria-label="Permalink to &quot;Default bindings&quot;">​</a></h2><p>Keys are identified by <a href="https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/code" target="_blank" rel="noreferrer"><code>KeyboardEvent.code</code></a> (layout-independent — <code>KeyW</code> is the same physical key on QWERTY and AZERTY).</p><table tabindex="0"><thead><tr><th>Action</th><th>Default key</th></tr></thead><tbody><tr><td><code>moveForward</code></td><td><code>W</code> (<code>KeyW</code>)</td></tr><tr><td><code>moveBack</code></td><td><code>S</code> (<code>KeyS</code>)</td></tr><tr><td><code>moveLeft</code></td><td><code>A</code> (<code>KeyA</code>)</td></tr><tr><td><code>moveRight</code></td><td><code>D</code> (<code>KeyD</code>)</td></tr><tr><td><code>jump</code></td><td><code>Space</code></td></tr><tr><td><code>sprint</code></td><td>left <code>Shift</code> (<code>ShiftLeft</code>)</td></tr></tbody></table><h2 id="rebinding" tabindex="-1">Rebinding <a class="header-anchor" href="#rebinding" aria-label="Permalink to &quot;Rebinding&quot;">​</a></h2><p>Bindings are plain data. <code>rebind(bindings, action, code)</code> returns a <strong>new</strong> map with the action pointed at a new key; if that key was already bound to another action, the old binding is cleared so one physical key never drives two actions. Cleared bindings are unbound and never match during reverse lookup. At runtime, <code>controller.setBinding(action, code)</code> delegates to the <a href="../../src/game/settings/settingsStore.ts">settings store</a> so changes persist and sync across live controllers. The <a href="./settings">Settings panel</a> drives the same path.</p><h2 id="pointer-lock-and-mouselook" tabindex="-1">Pointer lock and mouselook <a class="header-anchor" href="#pointer-lock-and-mouselook" aria-label="Permalink to &quot;Pointer lock and mouselook&quot;">​</a></h2><p>Clicking the canvas calls <code>requestPointerLock()</code>. While locked, raw <code>mousemove</code> deltas (<code>movementX</code>/<code>movementY</code>) feed <code>lookDX</code>/<code>lookDY</code>; when not locked, mouse movement is ignored. Losing the lock (<code>pointerlockchange</code> → not locked) triggers <code>releaseAll</code>, so no key stays stuck &quot;held&quot; after the player tabs away.</p><h3 id="esc-and-pause" tabindex="-1">ESC and pause <a class="header-anchor" href="#esc-and-pause" aria-label="Permalink to &quot;ESC and pause&quot;">​</a></h3><p>The browser <strong>reserves <code>ESC</code> to exit Pointer Lock</strong>, and JavaScript cannot intercept that keydown. To avoid a conflict, the input system <strong>never binds <code>ESC</code></strong> as a gameplay action. The app shell owns the coarse menu state instead: <code>Escape</code> toggles <code>playing ⇄ paused</code> in the Redux <code>app</code> slice, while <code>menu</code> ignores it. When pointer lock is wired into the playable scene, losing pointer lock should dispatch the same pause transition so physical ESC and browser unlock behavior stay aligned.</p><p>The menu overlay exposes only actions that work in the current slice. <code>New Game</code> is the primary focused action on boot. The pause overlay focuses <code>Resume</code>, and also provides a clickable <code>Quit to Main Menu</code> path so players do not need to rely on Escape.</p><h2 id="usage-sketch" tabindex="-1">Usage sketch <a class="header-anchor" href="#usage-sketch" aria-label="Permalink to &quot;Usage sketch&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createInputController } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/input&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> controller</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createInputController</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(canvas)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// each frame:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> intent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> controller.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sample</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">character.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(intent.moveX, intent.moveY, intent.sprint)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">camera.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rotate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(intent.lookDX, intent.lookDY)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// on teardown:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">controller.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dispose</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div>`,21)])])}const u=t(n,[["render",d]]);export{k as __pageData,u as default};
