import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.BYlK8ftn.js";const g=JSON.parse('{"title":"Enemy AI — Empire Soldier (E2.3) & Ranged Archer (FLO-432)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/enemy-ai.md","filePath":"guide/enemy-ai.md","lastUpdated":1782030101000}'),r={name:"guide/enemy-ai.md"};function n(o,e,d,l,h,c){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="enemy-ai-—-empire-soldier-e2-3-ranged-archer-flo-432" tabindex="-1">Enemy AI — Empire Soldier (E2.3) &amp; Ranged Archer (FLO-432) <a class="header-anchor" href="#enemy-ai-—-empire-soldier-e2-3-ranged-archer-flo-432" aria-label="Permalink to &quot;Enemy AI — Empire Soldier (E2.3) &amp; Ranged Archer (FLO-432)&quot;">​</a></h1><p>The game ships two enemy archetypes, each a deterministic finite-state machine (FSM): the melee <strong>Empire soldier</strong> (E2.3) and the ranged <strong>Empire archer</strong> (FLO-432). Pure game logic lives in <code>src/game/ai/</code>; the Babylon mesh wrappers in <code>src/scenes/soldierEnemy.ts</code> and <code>src/scenes/archerEnemy.ts</code>. Both reuse the same <code>patrol → engage → dead</code> scaffolding, the same <code>HealthState</code> damage funnel, and the same FLO-412 patrol leash — the archer is a <em>behaviour</em> variant, not a new AI engine. The soldier is documented first; the archer section follows.</p><h2 id="behaviour-states" tabindex="-1">Behaviour states <a class="header-anchor" href="#behaviour-states" aria-label="Permalink to &quot;Behaviour states&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>patrol ──[player inside detectionRadius]──▶ chase</span></span>
<span class="line"><span>chase  ──[player inside attackRadius]────▶ attack</span></span>
<span class="line"><span>chase  ──[player escapes 1.3× detection]──▶ patrol</span></span>
<span class="line"><span>attack ──[player outside 1.5× attackRadius]▶ chase</span></span>
<span class="line"><span>order  ──[follow/hold/move-to/attack-target]▶ ordered behaviour</span></span>
<span class="line"><span>order cleared ─────────────────────────────▶ patrol</span></span>
<span class="line"><span>any    ──[HP == 0]───────────────────────▶ dead  (terminal)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Phase</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>patrol</strong></td><td>Wander at 1.5 m/s; change direction every 3 s. <strong>Leashed</strong> to within <code>patrolLeashRadius</code> (6 m) of the spawn anchor — once it drifts past, it steers straight back, so a patrol guards its post instead of wandering off it (FLO-412). The wander direction is deterministic (golden-angle rotation, no wall-clock seed).</td></tr><tr><td><strong>chase</strong></td><td>Move toward player at 3 m/s</td></tr><tr><td><strong>attack</strong></td><td>Deal 15 HP every 1.5 s to the player</td></tr><tr><td><strong>follow</strong></td><td>Follow a commander until inside the follow distance</td></tr><tr><td><strong>hold</strong></td><td>Hold position until a new order arrives or the order is cleared</td></tr><tr><td><strong>move-to</strong></td><td>Move to an ordered destination, then hold</td></tr><tr><td><strong>attack-target</strong></td><td>Attack the ordered hostile target; if it disappears or dies, hold</td></tr><tr><td><strong>dead</strong></td><td>Stop all behaviour; the scene converts it to a persistent corpse — see <a href="./corpses">corpses.md</a> (E2.4)</td></tr></tbody></table><h2 id="default-parameters" tabindex="-1">Default parameters <a class="header-anchor" href="#default-parameters" aria-label="Permalink to &quot;Default parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Max HP</td><td>60</td></tr><tr><td>Detection radius</td><td>10 m</td></tr><tr><td>Attack radius</td><td>1.8 m</td></tr><tr><td>Attack damage</td><td>15 HP</td></tr><tr><td>Attack cooldown</td><td>1.5 s</td></tr><tr><td>Patrol speed</td><td>1.5 m/s</td></tr><tr><td>Patrol leash radius</td><td>6 m</td></tr><tr><td>Chase speed</td><td>3.0 m/s</td></tr><tr><td>Follow distance</td><td>2.5 m</td></tr><tr><td>Move-to arrival radius</td><td>0.35 m</td></tr></tbody></table><h2 id="pure-fsm-api" tabindex="-1">Pure FSM API <a class="header-anchor" href="#pure-fsm-api" aria-label="Permalink to &quot;Pure FSM API&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createSoldierFSM, stepSoldierFSM, applyDamageToSoldier } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/ai&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createSoldierFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Each fixed step (pass the spawn anchor as the last arg to leash the patrol):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDZ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">attacked</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  stepSoldierFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, soldierPos, playerPos, dt, params, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, orderContext, anchorPos)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// When player melee hits:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyDamageToSoldier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="commander-orders-e4-3" tabindex="-1">Commander orders (E4.3) <a class="header-anchor" href="#commander-orders-e4-3" aria-label="Permalink to &quot;Commander orders (E4.3)&quot;">​</a></h2><p>Pure command intake lives in <code>src/game/ai/orders.ts</code>. A commander issues one of four order types to same-faction subordinates:</p><table tabindex="0"><thead><tr><th>Order</th><th>Intake validation</th><th>Soldier behaviour</th></tr></thead><tbody><tr><td><code>follow</code></td><td>Recipient must be alive, same faction, and assigned to the commander</td><td>Move toward the commander until inside <code>followDistance</code></td></tr><tr><td><code>hold</code></td><td>Same recipient checks</td><td>Stop moving and wait</td></tr><tr><td><code>move-to</code></td><td>Same recipient checks; destination must be finite</td><td>Move to the destination, then transition to <code>hold</code></td></tr><tr><td><code>attack-target</code></td><td>Same recipient checks; target must exist, be alive, and be hostile by <code>resolveStance</code></td><td>Move into attack radius, deal ordered-target damage, then hold if the target is gone</td></tr></tbody></table><p>The boundary is deliberate: <code>issueSquadOrder()</code> rejects invalid recipients, destinations, and non-hostile targets up front. <code>SoldierEnemy</code> and <code>stepSoldierFSM()</code> then consume a compact <code>SoldierOrderContext</code> and trust it, instead of re-running faction checks every frame. When an order is cleared, an ordered soldier resumes default patrol/aggro behaviour.</p><p><code>?dev=orders</code> opens a standalone commander playground. It scripts the same intake path through <code>follow</code>, <code>move-to</code>, and <code>attack-target</code> orders against a hostile target, without changing default forest gameplay.</p><h2 id="damageable-contract" tabindex="-1">Damageable contract <a class="header-anchor" href="#damageable-contract" aria-label="Permalink to &quot;Damageable contract&quot;">​</a></h2><p><code>SoldierEnemy</code> implements the <code>Damageable</code> interface from E2.2 (melee combat). The player&#39;s <code>getMeleeHits</code> call returns living <code>SoldierEnemy</code> instances; the caller invokes <code>takeDamage(amount)</code> on each.</p><h2 id="fight-loop-forestscene-wiring" tabindex="-1">Fight loop (ForestScene wiring) <a class="header-anchor" href="#fight-loop-forestscene-wiring" aria-label="Permalink to &quot;Fight loop (ForestScene wiring)&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>render loop:</span></span>
<span class="line"><span>  1. stepMeleeAttack — advance player swing state</span></span>
<span class="line"><span>  2. if hitWindowOpen → getMeleeHits → takeDamage on each hit soldier</span></span>
<span class="line"><span>  3. loop.advance → runs SoldierEnemy.update → stepSoldierFSM</span></span>
<span class="line"><span>  4. if attacked → onPlayerDamaged(15) → dispatch(damagePlayer(15))</span></span>
<span class="line"><span>  5. if player HP == 0 → App.tsx death handler → returnToMenu</span></span></code></pre></div><h2 id="mesh" tabindex="-1">Mesh <a class="header-anchor" href="#mesh" aria-label="Permalink to &quot;Mesh&quot;">​</a></h2><p><code>SoldierEnemy</code> carries an invisible capsule as its position/collision proxy and mounts the <strong>FLO-311 Empire soldier GLB</strong> (<code>/models/empire-soldier.glb</code>) onto it, mirroring how the hero model is parented to the player capsule. The GLB load is best-effort: if the model can&#39;t be fetched (e.g. headless tests), the bare capsule placeholder stays visible. The FLO-311 GLB is a preview mesh with no baked texture pass, so <code>applyEmpireSoldierTexture()</code> assigns an in-engine Empire palette (green coat, brown leather, muted metal) to loaded submeshes and the placeholder capsule uses the same coat tone. Pass <code>glbUrl: null</code> to force the placeholder. On death the mesh topples procedurally and stays in place for E2.4 to take over.</p><h2 id="spawn" tabindex="-1">Spawn <a class="header-anchor" href="#spawn" aria-label="Permalink to &quot;Spawn&quot;">​</a></h2><p>Zone scenes spawn soldiers from local placement tables and register each <code>SoldierEnemy</code> with the fixed-step scheduler. MPG.5 guarantees the Forest boots with at least 5 soldier patrols and Human Lands boots with at least 3; Y is the capsule half-height (<code>0.9</code>) so each patrol rests on the ground.</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><p><code>src/game/ai/orders.test.ts</code> covers command intake validation. <code>src/game/ai/soldierFSM.test.ts</code> covers default FSM transitions plus ordered follow/hold/move/attack behaviours. <code>src/scenes/ordersPlayground.test.ts</code> smoke-tests the dev scene wiring, and <code>src/scenes/soldierEnemy.test.ts</code> covers the scene wrapper plus the in-engine soldier material palette.</p><hr><h1 id="ranged-archer-flo-432" tabindex="-1">Ranged Archer (FLO-432) <a class="header-anchor" href="#ranged-archer-flo-432" aria-label="Permalink to &quot;Ranged Archer (FLO-432)&quot;">​</a></h1><p>The second archetype is an Empire archer that <strong>keeps its distance and looses arrows</strong> instead of closing to melee. It reuses the soldier&#39;s FSM scaffolding — the same phase machine, the seed-free golden-angle patrol wander, the FLO-412 spawn-anchor leash, and the <code>createHealth</code>/<code>applyDamage</code> funnel — so it is a behaviour variant, not a new engine. Pure logic: <code>src/game/ai/rangedArcherFSM.ts</code>; Babylon wrapper: <code>src/scenes/archerEnemy.ts</code>.</p><h2 id="behaviour-states-1" tabindex="-1">Behaviour states <a class="header-anchor" href="#behaviour-states-1" aria-label="Permalink to &quot;Behaviour states&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>patrol ──[player inside detectionRadius]──▶ engage</span></span>
<span class="line"><span>engage ──[player escapes 1.3× detection]──▶ patrol</span></span>
<span class="line"><span>engage:  too close (&lt; minRange) ─────────▶ back-pedal (kite)</span></span>
<span class="line"><span>engage:  too far / no line of sight ─────▶ close the gap</span></span>
<span class="line"><span>engage:  in range + clear shot + off CD ─▶ fire an arrow</span></span>
<span class="line"><span>any    ──[HP == 0]───────────────────────▶ dead  (terminal → corpse)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Phase</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>patrol</strong></td><td>Identical to the soldier: golden-angle wander, leashed to <code>patrolLeashRadius</code> (6 m) of the spawn anchor (FLO-412).</td></tr><tr><td><strong>engage</strong></td><td><strong>Standoff kiting.</strong> Hold <code>preferredRange</code> (8 m): back-pedal when the player crowds inside <code>minRange</code> (5 m), edge closer when beyond preferred range or when the shot is blocked. Loose an arrow on <code>attackCooldown</code> (2 s) when the player is inside <code>engageRange</code> (12 m) <strong>and</strong> there is a clear line of sight. The archer never enters melee range on its own.</td></tr><tr><td><strong>dead</strong></td><td>Stop all behaviour; the scene converts it to a persistent corpse (its own ranger GLB) — see <a href="./corpses">corpses.md</a>.</td></tr></tbody></table><h2 id="default-parameters-1" tabindex="-1">Default parameters <a class="header-anchor" href="#default-parameters-1" aria-label="Permalink to &quot;Default parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>Value</th><th>Note</th></tr></thead><tbody><tr><td>Max HP</td><td>40</td><td>Squishier than the 60-HP soldier — a glass-cannon skirmisher</td></tr><tr><td>Detection radius</td><td>15 m</td><td>Sees farther than the melee soldier (10 m)</td></tr><tr><td>Engage range</td><td>12 m</td><td>Must be this close to be shot at</td></tr><tr><td>Preferred range</td><td>8 m</td><td>The standoff distance it tries to hold</td></tr><tr><td>Min range</td><td>5 m</td><td>Closer than this → it back-pedals</td></tr><tr><td>Attack damage</td><td>12 HP</td><td>Per arrow</td></tr><tr><td>Attack cooldown</td><td>2.0 s</td><td>Draw + nock time</td></tr><tr><td>Patrol speed</td><td>1.2 m/s</td><td></td></tr><tr><td>Reposition speed</td><td>2.4 m/s</td><td>Kiting / closing the gap</td></tr><tr><td>Projectile speed</td><td>20 m/s</td><td>Handed to the spawned arrow</td></tr></tbody></table><h2 id="line-of-sight" tabindex="-1">Line of sight <a class="header-anchor" href="#line-of-sight" aria-label="Permalink to &quot;Line of sight&quot;">​</a></h2><p>The archer only fires with a clear shot. The pure helper <code>lineOfSightClear(from, to, obstacles, padding?)</code> (<code>src/game/combat/lineOfSight.ts</code>) is a segment-vs-circle test on the XZ plane: obstacles are flat ground circles (tree trunks, hut footprints), height ignored. The FSM consumes a single <code>hasLineOfSight</code> boolean; <code>ArcherEnemy</code> computes it from an optional <code>hasLineOfSight(from, to)</code> callback (defaults to always-clear open ground). With no clear shot the archer holds fire and repositions to regain it.</p><h2 id="projectiles-the-damage-funnel" tabindex="-1">Projectiles &amp; the damage funnel <a class="header-anchor" href="#projectiles-the-damage-funnel" aria-label="Permalink to &quot;Projectiles &amp; the damage funnel&quot;">​</a></h2><p>Arrows are a pure model in <code>src/game/combat/projectile.ts</code>. A <code>ProjectileField</code> holds at most <code>MAX_LIVE_PROJECTILES</code> (24) live arrows — a hard <strong>budget cap</strong>: excess shots are dropped so the frame cost can&#39;t grow unbounded. Each tick <code>stepProjectileField</code> advances every arrow, decays its time-to-live, and reports contacts against the registered targets <em>without</em> mutating them (mirroring <code>getMeleeHits</code>).</p><p>The Babylon side is <code>ArrowVolley</code> (<code>src/scenes/arrowVolley.ts</code>), a <code>System</code> registered in the fixed-step loop. On each archer&#39;s <code>onFire</code> it spawns an arrow; each frame it resolves hits and calls <code>target.takeDamage(...)</code> on the player — the <strong>same <code>Damageable</code> funnel</strong> the player&#39;s melee uses. The player target&#39;s <code>takeDamage</code> applies the P7.1 spawn-grace scale and fires the <code>damageEvents</code> juice bridge (hurt SFX + camera shake via <code>emitShake</code>), so no ranged-specific feedback is special-cased. A capped mesh pool recycles one thin cylinder per live arrow.</p><h2 id="balance-seeding-p7-1" tabindex="-1">Balance &amp; seeding (P7.1) <a class="header-anchor" href="#balance-seeding-p7-1" aria-label="Permalink to &quot;Balance &amp; seeding (P7.1)&quot;">​</a></h2><p>Forest archers are seeded <strong>behind the melee soldier wave</strong> — both anchors sit ~34 m out, past every soldier cluster and well beyond <code>SAFE_SPAWN_BUFFER</code> (18 m) and the archer&#39;s 15 m detection radius — so a New Game player meets a melee front before any arrows fly and never aggros an archer from spawn. Arrow damage runs through the same spawn-grace window as soldier melee.</p><h2 id="mesh-corpse" tabindex="-1">Mesh &amp; corpse <a class="header-anchor" href="#mesh-corpse" aria-label="Permalink to &quot;Mesh &amp; corpse&quot;">​</a></h2><p><code>ArcherEnemy</code> mounts the <strong>FLO-426 ranged-archer GLB</strong> (<code>/models/ranged-archer.glb</code>) on an invisible capsule, best-effort (the capsule is the headless/fallback). <code>applyArcherTexture()</code> assigns a hooded-ranger palette (mossy cloak, tan leather, bow-wood) so it reads apart from the green-coated musketeer. On death the archer falls into a persistent corpse via the shared <code>CorpseManager</code>, using a per-corpse GLB override (FLO-432) so a fallen archer keeps the ranger silhouette rather than borrowing the soldier body.</p><h2 id="pure-fsm-api-1" tabindex="-1">Pure FSM API <a class="header-anchor" href="#pure-fsm-api-1" aria-label="Permalink to &quot;Pure FSM API&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createArcherFSM, stepArcherFSM, applyDamageToArcher } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/ai&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createArcherFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDZ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fired</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">aimDirX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">aimDirZ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  stepArcherFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, archerPos, playerPos, dt, params, { hasLineOfSight, anchorPos })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// if (fired) → spawn a projectile heading (aimDirX, aimDirZ)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyDamageToArcher</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// player melee hit</span></span></code></pre></div><h2 id="tests-1" tabindex="-1">Tests <a class="header-anchor" href="#tests-1" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><p><code>src/game/ai/rangedArcherFSM.test.ts</code> covers engage transitions, firing (range, cooldown, line-of-sight gating), standoff kiting, death, and the patrol leash. <code>src/game/combat/projectile.test.ts</code> covers arrow travel, the budget cap, and hit resolution; <code>src/game/combat/lineOfSight.test.ts</code> the segment-vs-circle geometry. <code>src/scenes/archerEnemy.test.ts</code> and <code>src/scenes/arrowVolley.test.ts</code> cover the scene wrappers (firing, LOS hold, kiting, death) and the projectile→<code>Damageable</code> funnel.</p>`,45)])])}const m=a(r,[["render",n]]);export{g as __pageData,m as default};
